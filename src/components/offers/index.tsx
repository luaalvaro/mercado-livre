import { Box } from '@chakra-ui/react'

const Offers: React.FC = ({ children }) => {
    return (
        <Box
            px="10px"
        >
            {children}
        </Box>
    )
}

export default Offers
